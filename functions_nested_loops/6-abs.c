#include "main.h"
/**
 * _abs - return the absolute value
 * @n: is the integer
 * Return: -n if n is negative
 * n if n is positive
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
