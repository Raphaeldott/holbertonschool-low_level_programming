#include <stdio.h>
/**
 * main - entry point of the program
 * prints a string
 * Return: always 0 (success)
 */
int main(void)
{
char *str = "Programming is like building a multilingual puzzle";
puts(str);
return (0);
}
/* Puts just print a string of characters */
