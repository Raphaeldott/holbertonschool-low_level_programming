#include <stdio.h>
/**
 * main - entry point of the program
 * printf is printing the string
 * Return: always 0 (success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
/* this function print a string */
